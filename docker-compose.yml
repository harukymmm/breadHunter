version: '3'
services:
  # front:
  #   container_name: front
  #   tty: true
  #   build: ./myapp
  #   volumes:
  #     - ./myapp/project:/myapp
  #   command: yarn start 
  #   ports:
  #     - 19000:19000
  #     - 19001:19001
  #     - 19002:19002d
  python:
    container_name: django
    tty: true
    ports:
     - "5001:5000"
    build: ./python
    volumes:
      - ./python:/python
    command: flask run --host=0.0.0.0
  sqlite:
    container_name: DB
    tty: true
    build: ./DB
    volumes:
      - ./DB:/sqlite

